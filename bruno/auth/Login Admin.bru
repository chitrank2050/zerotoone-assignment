meta {
  name: Login Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{apiPrefix}}/{{apiVersion}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password123"
  }
}

script:post-response {
  if (res.status === 201 || res.status === 200) {
    bru.setEnvVar("accessToken", res.body.access_token);
  }
}
